[build]
builder = "nixpacks"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["nodejs_22"]

[deploy]
startCommand = "node --experimental-sqlite src/server-entry.js"
restartPolicyType = "always"

[cron.backup]
schedule = "0 2 * * *"
command = "node scripts/backup.mjs"
